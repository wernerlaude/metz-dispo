<%= form_with(model: vehicle) do |form| %>
  <% if vehicle.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(vehicle.errors.count, "error") %> prohibited this vehicle from being saved:</h2>

      <ul>
        <% vehicle.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :license_plate, style: "display: block" %>
    <%= form.text_field :license_plate %>
  </div>

  <div>
    <%= form.label :vehicle_number, style: "display: block" %>
    <%= form.text_field :vehicle_number %>
  </div>

  <div>
    <%= form.label :vehicle_type, style: "display: block" %>
    <%= form.text_field :vehicle_type %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
