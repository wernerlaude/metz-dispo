<%= form_with(model: trailer) do |form| %>
  <% if trailer.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(trailer.errors.count, "error") %> prohibited this trailer from being saved:</h2>

      <ul>
        <% trailer.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :kennzeichen, style: "display: block" %>
    <%= form.text_field :kennzeichen %>
  </div>

  <div>
    <%= form.label :art, style: "display: block" %>
    <%= form.number_field :art %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
